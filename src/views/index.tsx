import MainLayouts from "../layouts";
import HomeModules from "../modules/Home";

const Home = () => {
  return (
    <MainLayouts>
      <HomeModules />
    </MainLayouts>
  );
};

export default Home;
